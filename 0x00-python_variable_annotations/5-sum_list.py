#!/usr/bin/env python3
"""
Import the necessary module.
"""


def sum_list(input_list: List[float]) -> float:
    """function to sum all elements in a list.

    Args:
        input_list: A list containing floating-point numbers (List[float]).

    Returns:
        The sum of all elements in the list (float).
    """
    return sum(input_list)
